[project]
name = "machine-designs-machine"
version = "0.1.0"
description = "RISC-V CPU core design project"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["tools/*"]
