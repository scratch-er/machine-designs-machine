[project]
name = "semantic-search"
version = "0.1.0"
description = "Semantic search over project specifications"
dependencies = [
    "requests>=2.28",
    "numpy>=1.24",
]

[tool.uv]
package = false
